-- SELECT name FROM people WHERE people.id = (SELECT stars.person_id FROM stars WHERE (stars.movie_id = (SELECT movies.id FROM movies WHERE (movies.year = 2004)))) ORDER BY birth;
SELECT name FROM people JOIN stars ON people.id = stars.person_id JOIN movies ON movies.id = stars.movie_id WHERE (movies.year = 2004) ORDER BY birth;